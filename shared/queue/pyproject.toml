[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-employee-shared-queue"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "redis",
]

[tool.setuptools]
packages = ["shared.queue"]
package-dir = {"shared.queue" = "."}
